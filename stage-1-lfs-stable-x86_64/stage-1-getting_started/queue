# проверяем host-систему на наличие всех нужных компонентов и их версий для
# сборки LFS системы
check-host-system-requirements.sh

# подготавливаем раздел жесткого диска на котором будем собирать LFS систему:
#    - форматирование разделов для LFS
#    - монтирование разделов
#    - создание минимального необходимого набора каталогов корневой системы LFS

###
# Важно
###
# Диск для сборки LFS системы был разбит таким образом:
#    sda
#    |-sda1      100M    /boot      * (bootable)
#    |-sda2      1G      [SWAP]
#    |-sda3      1K      [Extended]
#    |  -sda5    70G     /
#    |  -sda6    150G    /home
#    |  -sda7    30G     /tmp
#    |  -sda8    20G     [HOST]
#    |  -sda9    27G     [empty]

#    sda{1,7} - LFS
#    sda8     - Host система (Slackware_x86-64)
#    sda9     - пустой резервный раздел
#
# В скрипте ./partitions-init.sh устанавливаем переменную PART - корневой
# раздел для сборки LFS системы. Данный раздел будет монтироваться в /mnt/lfs
# В данном случае PART="/dev/sda5"
partitions-init.sh

# создание группы и пользователя lfs, под которым будем собирать временные
# инструменты для сборки LFS системы, а так же настройка рабочего окружения для
# этого пользователя (~/.bash_profile, ~/.bashrc)
creating-lfs-user.sh

# скачиваем в /mnt/lfs/sources все нужные исходники и патчи для сборки
download-src.sh

# vim:ft=conf
